#include <bits/stdc++.h>

#include "local_search.h"

using namespace std;

void simple_search(string filename, int badify) {
	local_search_improver l(filename, badify);
	// cout << (s.verify() ? "verified" : "bad") << endl;
	// s.save_if_better_than_default();
}
