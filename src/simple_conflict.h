#include <bits/stdc++.h>

#include "conflict_optimizer.h"

using namespace std;

void simple_conflict(string filename) {
	conflict_improver c(filename);
	// cout << (s.verify() ? "verified" : "bad") << endl;
	// s.save_if_better_than_default();
}
